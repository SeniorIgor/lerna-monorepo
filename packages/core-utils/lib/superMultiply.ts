import { multiply } from 'core';

export function partialMultiply(first: number): number {
  return multiply(first, 5);
}

export function partialMultiply1(first: number): number {
  return multiply(first, 5);
}
