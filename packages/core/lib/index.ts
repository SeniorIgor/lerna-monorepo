export function isOdd(n: number): boolean {
  return n % 2 === 1;
}

export function someFunction(n: number): boolean {
  return n % 2 === 1;
}

export function someFunction1(n: number): boolean {
  return n % 2 === 1;
}
