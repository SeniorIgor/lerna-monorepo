export function multiply(first: number, second: number): number {
  return first * second;
}

export function multiply2(first: number, second: number): number {
  return first * second;
}

export function multiply3(first: number, second: number): number {
  return first * second;
}

export function multiply4(first: number, second: number): number {
  return second * first;
}

export function multiply5(n: number, m: number): number {
  return n * m;
}

export function multiply6(m: number, n: number): number {
  return m * n;
}

export function multiply7(m: number, n: number): number {
  return m * n;
}

export function multiply8(m: number, n: number): number {
  return m * n;
}
